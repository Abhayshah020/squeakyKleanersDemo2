import type { Metadata } from "next";
import "./globals.css";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Squeaky Kleaners | Professional Cleaning Services in Australia",
  description:
    "Residential and Industrial Cleaning Services — Squeaky Kleaners delivers spotless results across Australia. Book your cleaning today!",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-white text-gray-900`}>
        {children}
      </body>
    </html>
  );
}
